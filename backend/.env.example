DB_USERNAME=
DB_PASSWORD=

MONGO_URI=

# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=

PORT=3000

# Comma-separated list of allowed origins for CORS, e.g.:
# ALLOWED_ORIGINS=http://localhost:8081,https://your-production-domain.com
# Leave unset during local development to allow all origins.
ALLOWED_ORIGINS=
